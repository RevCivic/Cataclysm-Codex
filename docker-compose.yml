services:
  codex:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cataclysm-codex
    ports:
      - "3000:3000"
    volumes:
      # Persist the JSON database across container restarts
      - codex-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/db.json
      # Required to enable source imports in production. Set this in .env or your secret store.
      - ADMIN_TOKEN=${ADMIN_TOKEN:-}
      - SOURCE_SNAPSHOT_PATH=/app/data/source-snapshots
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  codex-data:
    driver: local
